# sit323-737-2023-t1-prac4p

sit323/737-2023-t1-prac4p

Accepts following API endpoints

`/api/add`
Requires two variables `a` and `b`
returns `a+b`

`/api/subtract`
Requires two variables `a` and `b`
returns `a-b`

`/api/divide`
Requires two variables `a` and `b`
returns `a/b`

`/api/multiply`
Requires two variables `a` and `b`
returns `a*b`
